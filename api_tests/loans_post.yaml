# Test generated by RoostGPT for test QuickCredit-Backend using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/loans"
          json: {}
          headers: {}
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                message: "New loan is created"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid_url"
          json: {}
          headers: {}
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Invalid URL"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/loans"
          json: {"key": "value&^%$#@"}
          headers: {}
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                message: "New loan is created"
