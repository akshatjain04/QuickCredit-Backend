# Test generated by RoostGPT for test QuickCredit-Backend using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  plugins:
    expect: {}
  payload:
    path: "repayments_parameters.csv"
    fields: 
      - "loanId"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/loans/{{ loanId }}/repayments"
          expect:
            - statusCode: 200
            - hasProperty: "response"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/loans/invalidLoanId/repayments"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/loans/12345678901234567890/repayments"
          expect:
            - statusCode: 200
            - hasProperty: "response"
