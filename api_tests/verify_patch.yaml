# Test generated by RoostGPT for test QuickCredit-Backend using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - patch:
          url: "/users/test@test.com/verify"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - patch:
          url: "/users/invalid/verify"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - patch:
          url: "/users/verylongemailaddresswithspecialcharacters@test.com/verify"
          expect:
            - statusCode: 200
